id: b3-cotahist

description: Dataset dos arquivos COTAHIST

etl:
  pipeline:
    - step: concat_datasets
      inputs:
        - b3-cotahist-yearly
        - b3-cotahist-daily
      layer: input

    - step: sort
      by: [refdate]

writer:
  layer: staging
